services:
  backend:
    build: ./backend/practicecodesignal
    ports:
      - "8080:8080"
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend